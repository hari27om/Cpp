find the largest element in the array.
  
1.Approach
  
  sort the array and the element at the last index is the ans.
  tc=O(nlogn)

2.Approach

  declare a variable and initalize to the first index, and then compare with each element.
  tc=O(n)